{
  "$schema": "../schemas/client-config.v1.schema.json",
  "profile": "canary",
  "notes": "One client, pinned ahead of everyone else. Step 4 of the staged rollout in selectors/README.md: a pack that validated is not evidence, and a pack that has run clean on a canary for a full cadence interval is. Pinned to v2 in PH-13, which is the first pack that declares the containers extraction needs. `standard` deliberately stays on v1 until v2 has a canary cycle behind it — widening on the strength of a green fixture corpus alone would skip step 5, which is the step that catches what fixtures cannot.",
  "selector_packs": {
    "google": "v2"
  },
  "cadence_floor_hours": 6,
  "nav": {
    "max_reviews": 1000
  }
}
